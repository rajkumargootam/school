<li>
  <%= student.name %>
  (
  <%= link_to("Edit", edit_student_path(student))  %>
  <%= link_to("Delete", student_path(student), method: :delete, data: {confirm: "Are you sure you want to delete the Class?"})  %>
  )
  </li>
