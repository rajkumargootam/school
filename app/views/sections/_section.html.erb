<li>
  <%= section.name %>
  (
  <%= link_to("Edit", edit_section_path(section))  %>
  <%= link_to("Delete", section_path(section), method: :delete, data: {confirm: "Are you sure you want to delete the Class?"})  %>
  )
  </li>
